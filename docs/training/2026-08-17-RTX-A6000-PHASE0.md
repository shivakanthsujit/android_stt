# RTX A6000 Phase 0 preflight

Date: 2026-08-17
Repository commit at preflight: `2ae244cf761d91846396b6f96161955e7666e3d5`
Host: `dante`

## Result

Phase 0 passes. The user ran the host checks directly on `dante` because the managed agent sandbox
cannot see NVIDIA device nodes or write `/data`. The exact CUDA/Python dependency set is locked and
synchronized, and the PyTorch BF16 smoke passed. Source download began only after this pass; no
model training was started.

## Recorded machine state

- OS/kernel: Ubuntu 22.04.5 LTS; Linux `5.15.0-176-generic`, x86_64.
- CPU: Intel Xeon Silver 4316, 20 online physical cores exposed.
- RAM: 125 GiB total; about 119 GiB available at preflight; 8 GiB swap.
- GPU on PCI bus: NVIDIA GA102GL RTX A6000 at `51:00.0`.
- Loaded kernel module: NVIDIA `550.144.03`.
- Direct host GPU check supplied by the user: NVIDIA-SMI `550.144.03`, CUDA compatibility `12.4`,
  RTX A6000 with 49,140 MiB, 31 MiB used, 0% utilization, 32 C, and 26 W / 300 W. Only Xorg and
  GNOME Shell used small allocations at inspection time.
- Repository/home volume: about 894 GiB total, 99% used, about 11 GiB free. It is unsuitable for
  model caches, source payloads, or run directories.
- Large artifact volume: `/data`, about 10.9 TiB total with about 9.6 TiB free. The user verified
  `/data/rise/android_stt/` is writable and created raw/work/reviews/cache/runs/models/manifests.
- Python: CPython 3.10.12 at `/usr/bin/python3`; `uv` is installed.
- Git: clean `main`, aligned with `origin/main` at preflight.
- Dependency resolution: host-side `uv lock --project training` resolved 77 packages under
  CPython 3.10.12. The reviewed lock selects PyTorch/Torchvision CUDA 12.4 wheels and exact
  Transformers/PEFT/Accelerate/Datasets versions. `uv sync --locked --no-dev` installed the lock
  under `/data/rise/android_stt/env`.
- Environment smoke: Python 3.10.12, PyTorch `2.6.0+cu124`, PyTorch CUDA 12.4, cuDNN 9.1,
  compute capability 8.6, one 49,140 MiB RTX A6000, and BF16 matmul all passed. The external,
  text-free report is 2,722 bytes with SHA-256
  `3c0b8b48ba9485c0e9064883010bb7886f42575cc0d5484d2fa36a1a779a1611`.
- Public Hugging Face and GitHub source endpoints succeeded. No token was printed or stored.
- Source snapshot: 14 files totaling 1,112,420,288 bytes were fetched from the three exact pins and
  reverified byte-for-byte. The external manifest SHA-256 is
  `500a62d09fb48e8e287cd01c46aa7e708cc3a3acd6c8079651a444e850645702`; its embedded config hash
  matches `training/config/sources-v1.json` at
  `d1c54a5ae5676b74c74b1db24b62cde30b262d6098ffdcdc87d996bda0864cde`.

## Baseline verification

- `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`: 93/93 passed after the V2
  broader-editor policy, pending supplement, cross-quota selection, optimized near-duplicate, and
  end-to-end Gate A fixture checkpoint.
- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py' -v`:
  10/10 passed.
- `git diff --check`: passed.

## Implemented before the permission boundary

- Immutable three-source config with the handoff revisions and license labels.
- Fail-closed Hugging Face/GitHub fetcher with revision verification, safe extraction, file byte
  counts, SHA-256 values, retrieval timestamps, and validated HTTP-range resume for interrupted
  large payloads.
- Public-source importer for Sotto, Disfl-QA, and Nyra schemas. Every imported row is pending;
  grammar, ASR-repair, mixed/crutch, lexical-addition, protected-literal, adversarial, and
  high-stakes rows remain available only through labeled human-review queues. Canonical Sotto
  Parquet avoids its overlapping legacy JSONL, and Nyra import projects only text/provenance
  columns while mapping filler tags without loading audio.
- Deterministic family/template and near-duplicate grouping before split assignment.
- Exact 5,000/500 pilot bucket targets, including 500/50 each for explicit formatting, grammar
  repair, and ASR repair, plus mixed/crutch and high-stakes cross-cutting checks.
- V2 dataset/annotation contract separating explicit transcript formatting controls from general
  commands, plus direct training/inference/scoring and read-only managed-run monitoring tools.
- Text-free real-source profiler plus a deterministic pending-only supplemental generator for the
  measured paragraph, adversarial, and Unicode/multilingual shortages. Rare cross-cutting rows are
  reserved before family grouping and selected against minima while primary quotas remain exact.
- Unit/integration coverage for archive safety, pinned source schemas/subsets, review, grouping,
  formatting, Gate A, assistant-only labels, output bounds, scoring, and monitoring deltas.

The code paths have synthetic unit/integration coverage, the real pinned fetch/config/file-hash
check passes, and a temporary end-to-end import maps 147,142 rows with zero source-native holdout
consumption. Durable `/data` import, human review, and Gate A remain incomplete.

## Required continuation

1. Keep using the user-approved `/data/rise/android_stt/` artifact root for all large artifacts.
2. Run importer schema/count/license and candidate-coverage audits.
3. Produce pending review queues. A human must approve the required rows; an automated agent may
   not mark them approved.
4. Build the 5,000/500 release pilot, run the committed validator and manifest round-trip, and
   commit the sanitized Gate A report before any GPU training.

Blind-v2 has not been created, read, or exposed. The pipeline has no blind-v2 input path.
