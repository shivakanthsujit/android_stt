[project]
name = "localflow-tts-tools"
version = "0.1.0"
description = "Pinned Mac-local TTS runtime for Local Flow evaluation audio"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mlx-audio==0.4.6",
]

[tool.uv]
package = false
